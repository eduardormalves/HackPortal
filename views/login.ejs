<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>HackPortal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      height: 100vh;
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #fdf8f4; /* tom de fundo das outras telas */
    }

    section {
      text-align: center;
      width: 100%;
      max-width: 380px;
      background-color: white;
      padding: 32px 30px 40px;
      border-radius: 16px;
      box-shadow: 0 6px 16px rgba(0,0,0,0.07);
      border: 1px solid #f57c00; /* borda laranja */
    }

    h1 {
      font-size: 36px;
      margin-bottom: 30px;
      color: #f57c00; /* laranja do HackPortal */
      font-weight: 700;
      letter-spacing: 1px;
    }

    .form-control {
      border: 1.5px solid #f57c00;
      border-radius: 12px;
      padding: 12px 15px;
      font-size: 1rem;
      transition: border-color 0.25s ease, box-shadow 0.25s ease;
      color: #333;
    }

    .form-control::placeholder {
      color: #a37a3a;
      opacity: 1;
    }

    .form-control:focus {
      border-color: #d26900;
      box-shadow: 0 0 8px rgba(245,124,0,0.4);
      outline: none;
    }

    .btn-primary {
      width: 100%;
      border-radius: 12px;
      font-weight: 700;
      background-color: #f57c00;
      border: none;
      padding: 12px 0;
      font-size: 1.1rem;
      cursor: pointer;
      box-shadow: 0 4px 10px rgba(245,124,0,0.5);
      transition: background-color 0.3s ease;
    }

    .btn-primary:hover {
      background-color: #d26900;
      box-shadow: 0 6px 14px rgba(210,105,0,0.6);
    }

    .radios {
      display: flex;
      justify-content: center;
      gap: 40px;
      margin-top: 20px;
      margin-bottom: 20px;
    }

    .form-check-label {
      margin-left: 6px;
      cursor: pointer;
      font-weight: 600;
      color: #333;
      user-select: none;
      font-size: 1rem;
    }

    .form-check-input {
      cursor: pointer;
      accent-color: #f57c00;
      width: 18px;
      height: 18px;
    }

    a {
      font-size: 0.9rem;
      display: block;
      text-align: right;
      margin-top: 8px;
      color: #f57c00;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s ease;
      cursor: pointer;
    }

    a:hover {
      color: #d26900;
      text-decoration: underline;
    }

    @media (max-width: 420px) {
      section {
        padding: 25px 20px 35px;
        max-width: 90vw;
      }

      .radios {
        gap: 20px;
      }
    }
  </style>
</head>
<body>

  <section>
    <h1>HackPortal</h1>
    <form action="/enviaLogin" method="post" autocomplete="off" novalidate>
      <input type="text" placeholder="Login" class="form-control my-3" name="login" required />
      <input type="password" placeholder="Senha" class="form-control my-3" name="senha" required />

      <div class="radios">
        <div class="form-check">
          <input class="form-check-input" type="radio" name="perfil" id="aluno" value="aluno" required>
          <label class="form-check-label" for="aluno">Aluno</label>
        </div>
        <div class="form-check">
          <input class="form-check-input" type="radio" name="perfil" id="professor" value="professor" required>
          <label class="form-check-label" for="professor">Professor</label>
        </div>
      </div>

      <input type="submit" value="Entrar" class="btn btn-primary" />

      <a href="#">Esqueci minha senha</a>
    </form>
  </section>

</body>
</html>
