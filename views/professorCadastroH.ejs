<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Cadastrar Hackton</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    /* Reset */
    * {
      box-sizing: border-box;
    }
    html, body {
      margin: 0; padding: 0;
      height: 100vh;
      width: 100vw;
      overflow: hidden;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #fffaf5;
      color: #222;
      display: flex;
      flex-direction: row;
    }
    nav {
      width: 320px;
      height: 100vh;
      background-color: #fff;
      border-right: 1px solid #eee;
      box-shadow: 2px 0 8px rgba(0,0,0,0.1);
      padding: 25px 30px;
      overflow-y: auto;
      flex-shrink: 0; /* não encolhe */
    }
    main {
      flex-grow: 1; /* ocupa todo espaço restante */
      height: 100vh;
      /* reduzimos o padding para não "roubar" tanto espaço */
      padding: 40px 30px;
      overflow-y: auto;
      background-color: #fffaf5;
      display: flex;
      flex-direction: column;
      /* vamos limitar a largura interna para o conteúdo, para ficar responsivo */
      max-width: calc(100vw - 320px);
    }
    h2 {
      color: #f57c00;
      font-weight: 700;
      font-size: 2.4rem;
      margin-bottom: 50px;
      letter-spacing: 0.8px;
      text-shadow: 0 1px 3px rgba(245,124,0,0.3);
    }
    form {
      /* Remove o max-width para expandir o formulário */
      width: 100%;
      background: #fff;
      padding: 30px 35px;
      border-radius: 14px;
      box-shadow: 0 6px 25px rgba(245,124,0,0.15);
      transition: box-shadow 0.3s ease;
      /* margem automática para centralizar, mas se quiser pode remover */
      margin: 0 auto;
      max-width: none; /* remove limite para não cortar */
    }
    form:hover {
      box-shadow: 0 8px 30px rgba(245,124,0,0.25);
    }
    label {
      font-weight: 600;
      color: #333;
      font-size: 1.05rem;
      margin-bottom: 6px;
      display: block;
      letter-spacing: 0.3px;
    }
    input.form-control,
    textarea.form-control {
      border: 2px solid #f57c00;
      border-radius: 14px;
      padding: 12px 18px;
      font-size: 1.1rem;
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
      box-shadow: inset 0 1px 4px rgba(0,0,0,0.05);
    }
    input.form-control:focus,
    textarea.form-control:focus {
      border-color: #d26900;
      box-shadow: 0 0 8px rgba(245,124,0,0.5);
      outline: none;
      background: #fff7e6;
    }
    .row > .mb-3,
    .row > .col-md-12 {
      margin-bottom: 28px;
    }
    button.btn-submit {
      background-color: #f57c00;
      border: none;
      font-weight: 700;
      font-size: 1.2rem;
      border-radius: 14px;
      padding: 14px 28px;
      box-shadow: 0 6px 20px rgba(245,124,0,0.4);
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
      width: 100%;
      max-width: 320px;
      align-self: center;
      cursor: pointer;
      color: white;
      letter-spacing: 0.6px;
    }
    button.btn-submit:hover {
      background-color: #d26900;
      box-shadow: 0 8px 30px rgba(245,124,0,0.6);
    }

    /* Responsividade */
    @media (max-width: 768px) {
      html, body {
        flex-direction: column;
        overflow: auto;
        height: auto;
        width: 100%;
      }
      nav {
        width: 100%;
        height: auto;
        box-shadow: none;
        border-right: none;
        padding: 20px 25px;
      }
      main {
        width: 100%;
        height: auto;
        padding: 30px 20px;
        max-width: 100vw; /* garantir que não exceda */
      }
      form {
        max-width: 100%;
        padding: 25px 20px;
        border-radius: 10px;
        margin: 0 auto;
      }
      button.btn-submit {
        max-width: 100%;
      }
    }
  </style>
</head>
<body>

  <%- include('navbar/navbarAluno.ejs') %>

  <main role="main">
    <form action="/enviaPCadastroH" method="POST" autocomplete="off" novalidate>
      <div class="row">
        <div class="mb-3">
          <label for="nome" class="form-label">Nome do Hackton</label>
          <input type="text" id="nome" name="nome" class="form-control" required />
        </div>
      </div>

      <div class="row">
        <div class="col-md-4 mb-3">
          <label for="dataInscricaoMax" class="form-label">Data Máxima de Inscrição</label>
          <input type="date" id="dataInscricaoMax" name="dataInscricaoMax" class="form-control" required />
        </div>
        <div class="col-md-4 mb-3">
          <label for="dataInicio" class="form-label">Data de Início</label>
          <input type="date" id="dataInicio" name="dataInicio" class="form-control" required />
        </div>
        <div class="col-md-4 mb-3">
          <label for="dataFim" class="form-label">Data de Término</label>
          <input type="date" id="dataFim" name="dataFim" class="form-control" required />
        </div>
      </div>

      <div class="row">
        <div class="col-md-12 mb-4">
          <label for="local" class="form-label">Local</label>
          <input type="text" id="local" name="local" class="form-control" required />
        </div>
      </div>

      <button type="submit" class="btn-submit">Cadastrar Hackton</button>
    </form>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
