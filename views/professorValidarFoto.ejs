<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Validação de Fotos - Professor</title>
  <style>
    /* Reset básico e fonte */
    * { box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #fffaf5;
      margin: 0; padding: 0;
      color: #222;
      height: 100vh;
      overflow: hidden;
    }
    .container {
      margin-left: 320px;
      padding: 40px 30px 30px;
      height: 100vh;
      overflow-y: auto;
    }
    h1 {
      color: #f57c00;
      margin-bottom: 50px; /* Aumentado espaçamento para melhor separação */
      font-weight: 700;
      font-size: 2.2rem;
    }
    section {
      margin-bottom: 40px;
    }
    section h2 {
      color: #f57c00;
      font-weight: 700;
      font-size: 1.8rem;
      border-bottom: 3px solid #f57c00;
      padding-bottom: 8px;
      margin-bottom: 24px;
    }

    /* Container dos cards de hacktons */
    .hackton-cards-container {
      display: flex;
      flex-direction: column;
      gap: 30px;
    }

    /* Card hackton */
    .hackton-card {
      background-color: #fff;
      border-radius: 14px;
      box-shadow: 0 6px 20px rgba(245, 124, 0, 0.15);
      padding: 25px 30px;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .hackton-header {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
    }

    .hackton-header h3 {
      color: #bf6200;
      font-weight: 700;
      font-size: 1.6rem;
      margin: 0;
      flex: 1 1 auto;
      min-width: 180px;
    }

    .hackton-date {
      color: #f57c00;
      font-weight: 600;
      font-size: 1rem;
      white-space: nowrap;
      flex: 0 0 auto;
    }

    .hackton-header form button.finalize-btn {
      background-color: #28a745;
      color: #fff;
      padding: 8px 22px;
      border-radius: 8px;
      border: none;
      font-weight: 600;
      cursor: pointer;
      flex: 0 0 auto;
      transition: background-color 0.25s ease;
    }
    .hackton-header form button.finalize-btn:hover {
      background-color: #218838;
    }

    /* Lista de participantes */
    .participantes-list {
      display: flex;
      flex-direction: column;
      gap: 14px;
      max-height: 280px;
      overflow-y: auto;
      padding-right: 5px;
    }

    .participante-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: #fffaf5;
      padding: 12px 18px;
      border-radius: 10px;
      box-shadow: 0 1px 6px rgba(0,0,0,0.05);
      gap: 15px;
      flex-wrap: wrap;
    }

    .participante-info {
      flex: 1 1 250px;
      color: #444;
      font-size: 1rem;
      line-height: 1.3;
    }

    .participante-actions {
      flex: 0 0 auto;
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .participante-actions button {
      background-color: #f57c00;
      color: white;
      border: none;
      padding: 7px 16px;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.25s ease;
      font-size: 0.9rem;
      user-select: none;
    }
    .participante-actions button:hover {
      background-color: #e06b00;
    }
    .participante-actions button.reject-btn {
      background-color: #cc3f00;
    }
    .participante-actions button.reject-btn:hover {
      background-color: #b23100;
    }

    /* Scrollbar personalizada */
    .participantes-list::-webkit-scrollbar {
      width: 8px;
    }
    .participantes-list::-webkit-scrollbar-track {
      background: #f3e9d3;
      border-radius: 4px;
    }
    .participantes-list::-webkit-scrollbar-thumb {
      background: #f57c00;
      border-radius: 4px;
    }

    /* Mensagem quando não há participantes */
    .no-participantes {
      font-style: italic;
      color: #999;
      font-size: 1rem;
      margin-top: 10px;
    }

    /* Modal da foto */
    #modalFoto {
      display: none;
      position: fixed;
      z-index: 999;
      left: 0; top: 0;
      width: 100vw; height: 100vh;
      background-color: rgba(0,0,0,0.6);
      justify-content: center;
      align-items: center;
    }
    #modalFoto img {
      max-width: 90vw;
      max-height: 90vh;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0,0,0,0.8);
    }
    #modalFoto .close-btn {
      position: absolute;
      top: 25px; right: 40px;
      font-size: 28px;
      font-weight: bold;
      color: white;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <%- include('navbar/navbarProfessor.ejs') %>

  <div class="container">

    <% if (!hacktonsPendentes.length && !hacktonsFinalizados.length) { %>
      <p>Não há hacktons para validar ou finalizados.</p>
    <% } %>

    <section>
      <h2>Hacktons Encerrados para Validação</h2>
      <% if (!hacktonsPendentes.length) { %>
        <p>Não há hacktons para validar.</p>
      <% } else { %>
        <div class="hackton-cards-container">
          <% hacktonsPendentes.forEach(hackton => { %>
            <div class="hackton-card">
              <div class="hackton-header">
                <h3><%= hackton.nome %></h3>
                <span class="hackton-date">Encerrado: <%= new Date(hackton.data_fim).toLocaleDateString('pt-BR') %></span>
                <form action="/finalizar-hackton/<%= hackton.id_hackton %>" method="POST">
                  <button type="submit" class="finalize-btn">Fechar</button>
                </form>
              </div>

              <% if (!hackton.participantes || hackton.participantes.length === 0) { %>
                <p class="no-participantes">Nenhum aluno enviou fotos para validação neste hackton.</p>
              <% } else { %>
                <div class="participantes-list">
                  <% hackton.participantes.forEach(participante => { %>
                    <div class="participante-item">
                      <div class="participante-info">
                        <strong><%= participante.nome_aluno %></strong><br />
                        <span><%= participante.email_edu %></span><br />
                        <small>Enviado em: <%= new Date(participante.data_envio).toLocaleDateString('pt-BR') %></small>
                      </div>
                      <div class="participante-actions">
                        <button class="view-btn" data-foto="<%= participante.caminho %>">Visualizar Foto</button>
                        <form action="/rejeitar-foto" method="POST" style="display:inline;">
                          <input type="hidden" name="id_foto" value="<%= participante.id_foto %>">
                          <input type="hidden" name="id_aluno" value="<%= participante.id_aluno %>">
                          <button type="submit" class="reject-btn">Rejeitar Foto</button>
                        </form>
                      </div>
                    </div>
                  <% }) %>
                </div>
              <% } %>
            </div>
          <% }) %>
        </div>
      <% } %>
    </section>

    <section>
      <h2>Hacktons Finalizados</h2>
      <% if (!hacktonsFinalizados.length) { %>
        <p>Não há hacktons finalizados.</p>
      <% } else { %>
        <div class="hackton-cards-container">
          <% hacktonsFinalizados.forEach(hackton => { %>
            <div class="hackton-card">
              <div class="hackton-header">
                <h3><%= hackton.nome %></h3>
                <span class="hackton-date">Finalizado em: <%= new Date(hackton.data_fim).toLocaleDateString('pt-BR') %></span>
              </div>
              <p>Este hackton foi encerrado e finalizado.</p>
              <% if (hackton.participantes && hackton.participantes.length > 0) { %>
                <div class="participantes-list">
                  <% hackton.participantes.forEach(participante => { %>
                    <div class="participante-item">
                      <div class="participante-info">
                        <strong><%= participante.nome_aluno %></strong><br />
                        <span><%= participante.email_edu %></span>
                      </div>
                    </div>
                  <% }) %>
                </div>
              <% } else { %>
                <p class="no-participantes">Nenhum participante registrado.</p>
              <% } %>
            </div>
          <% }) %>
        </div>
      <% } %>
    </section>
  </div>

  <div id="modalFoto">
    <span class="close-btn" onclick="fecharModal()">×</span>
    <img src="" alt="Foto do aluno" />
  </div>

  <script>
    const modal = document.getElementById('modalFoto');
    const modalImg = modal.querySelector('img');

    document.querySelectorAll('.view-btn').forEach(btn => {
      btn.addEventListener('click', e => {
        e.stopPropagation();
        const fotoPath = btn.getAttribute('data-foto');
        modalImg.src = '/Uploads/' + fotoPath;
        modal.style.display = 'flex';
      });
    });

    function fecharModal(e) {
      if(e) e.stopPropagation();
      modal.style.display = 'none';
      modalImg.src = '';
    }
  </script>
</body>
</html>
